# Teammate 1 — FoodZilla runs. Fill udid, platform_version, org_id, bundle_id before running.

device:
  name: "Member1 iPhone"
  udid: "REPLACE_WITH_MEMBER1_UDID"
  platform_version: "REPLACE_IOS_VERSION"

app:
  bundle_id: "REPLACE_FOODZILLA_BUNDLE_ID"

appium:
  server_url: "http://127.0.0.1:4723"

xcode:
  org_id: "REPLACE_APPLE_TEAM_ID"
  signing_id: "Apple Development"

session:
  wda_local_port: null
  new_command_timeout: 120
